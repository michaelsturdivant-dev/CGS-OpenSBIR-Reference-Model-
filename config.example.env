# Copy this file only for local development configuration.
# Never commit real secrets, tokens, credentials, keys, or private customer data.

APP_NAME=CGS Sentinel OpenSBIR
APP_ENV=development
DATABASE_URL=sqlite:///./data/opensbir.db
